"""Symphony LangSmith evaluation suite.

Run evaluations against the agent to track quality metrics over time.
Requires ``LANGCHAIN_API_KEY`` and ``LANGCHAIN_TRACING_V2=true`` in the
environment (or in the application ``.env`` file).
"""
